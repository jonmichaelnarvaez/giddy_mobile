import React from 'react';
import renderer from 'react-test-renderer';

// test for FlatList component for articles
it('render active articles feed', () => {})

// test for auth flow

// test for user delete data